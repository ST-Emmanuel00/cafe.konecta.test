import { db } from "../../common/config/index.js";

export const updateProduct = async () => {
    try {

        return {
            message: 'Update product jeje',
            data: {}
        }

    } catch (error) {
        throw error
    }
};